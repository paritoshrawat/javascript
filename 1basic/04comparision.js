console.log(null > 0); //false
console.log(null == 0); // false null is converted to zero but actually it is not zero

console.log(null >= 0); //true    null is converted to a number 0 so it is greater than 0 or equal to zero
